{
  "name": "edrift-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "E-Drift Trikes backend service (Render) — email, payment webhooks, contact, fulfillment scheduler.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "node-cron": "^3.0.3",
    "resend": "^4.0.1",
    "stripe": "^17.3.1"
  }
}
